<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>

  <link href="//fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/main.css">
  <link rel="shortcut icon" href="images/favicon.png" type="image/x-icon">
</head>
<body>
  <main class="main">
    <h1>🔗 URL Shortener</h1>

    <form class="form__shrink" action="/urls" method="POST">
      <input class="input__url" placeholder="URL" type="url" name="fullUrl" id="fullUrl" />
      <button class="btn btn__shrink" type="submit">Shrink</button>
    </form>

    <% if (urls.length === 0) { %>
      <p class="empty">
        🚫 No URL's were found.
      </p>
    <% } %>

    <% if (urls.length) { %>
      <table class="table">
        <thead>
          <tr>
            <th>Full URL</th>
            <th>Short URL</th>
            <th>Clicks</th>
          </tr>
        </thead>
        <tbody>
          <% urls.forEach(url => { %>
            <tr>
              <td>
                <a href="<%= url.full %>"><%= url.full %></a>
              </td>
              <td>
                <a href="<%= url.short %>"><%= url.short %></a>
              </td>
              <td>
                <%= url.clicks %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </main>
</body>
</html>
